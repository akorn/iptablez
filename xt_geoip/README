Run ./update.zsh; place a symlink to this directory in /usr/share (hardcoded in geoip xtables extension); and you're all set to use geoip based rules.

It's probably a good idea to re-run update.zsh occasionally (say, once a month).
